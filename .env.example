# Supabase (also mirrored in src/environments/environment.ts for Angular builds)
SUPABASE_URL=https://zveiywuiiyvokicmptxg.supabase.co
SUPABASE_ANON_KEY=

# Server-only: needed for scripts/upload-menu-images.mjs (never commit / never expose to the browser)
SUPABASE_SERVICE_ROLE_KEY=
